# SENGOKU-SHIELD 要件定義書（v0.2・フェーズ2）

作成日: 2026年9月3日（フェーズ2追記: 同日）

---

## 1. プロダクトの概要

| 項目 | 内容 |
|---|---|
| プロダクト名 | **SENGOKU-SHIELD** |
| 一言でいうと | 迷惑電話・詐欺電話をユーザーの代わりにAIが受け、時間を稼ぐ／証拠を残す防衛システム |
| 想定利用者 | まずは自分専用（将来的に他人にも配る・公開する想定） |
| 通話の受け方 | ユーザーのスマホから、この専用番号（Twilio）へ着信転送する方式 |

## 2. なぜ元の14モジュール仕様をそのまま実装しなかったか（重要）

ご依頼いただいた仕様書には、スマホ標準機能だけでは技術的に実現できない前提や、公開前提のプロダクトとして踏み込むと法的リスクが大きい部分が含まれていたため、実装に入る前に3点を確認し、以下の方針で進めています（会話内で確認済み）。

1. **通話の乗っ取り方式**: 「Siri/Google Assistantの音声コマンドで通話音声をそのままAIに渡す」はiOS/Android標準では不可能（Appleは第三者アプリの通話音声アクセスを原則禁止、Androidの`CallScreeningService`は着信の許可/拒否/サイレント化のみ）。→ **着信転送 + Twilio VoIP番号方式**に変更。Siri/Google Assistantは「転送のON/OFF切り替え」のショートカットとして使う（セクション6参照）
2. **SNS自動投稿**: 通話録音・電話番号をSNSへ自動投稿する仕様は、相手が本当に詐欺師であっても誤検知時に名誉毀損・プライバシー侵害のリスクがあるため、**手動確認必須**に変更（ダッシュボードでコピーする方式。自動投稿APIは実装していない）
3. **冒頭アナウンス文言**: 元の仕様「通話は録音・解析され、法務当局およびSNSに自動配信されます」は、実際にはやっていないこと（自動配信）を相手に伝える内容だったため、**事実に即した文言**に変更した（`lib/honeypot-prompt.ts`の`PRELUDE_MESSAGE`）。実際にやっていないことを「やる」と相手に告げるのは、脅迫的な虚偽告知として法的に問題になり得るため

## 3. 全体アーキテクチャ

```
着信 → ユーザーのスマホで転送 → Twilio番号
  → /api/voice/incoming（冒頭アナウンス + ホワイトリスト優先照合 + ブロックリスト照合）
      → ホワイトリスト一致: おとりAIを介さず案内のみで終了
  → /api/voice/turn（音声認識 → ペルソナ選択（honeypot / screening / honeypot_reverse_care）
                      → Claude API → 読み上げ、を繰り返す会話ループ）
  → /api/voice/status（通話終了を検知して記録を確定）

ダッシュボード（/、Basic認証保護）
  → 通話一覧・ライブ文字起こし（2秒ポーリング）・現在のペルソナ表示
  → 今すぐ切る（Twilio REST APIで即時終了）
  → 法的メモの下書き生成
  → 条件・注意点のチェック（相手の期限/金額、AIの同意事故フラグ）
  → SNS投稿文のコピー（手動確認必須、自動投稿なし）
  → ブロックリスト・ホワイトリストの追加・削除
```

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js（App Router）+ TypeScript + Tailwind CSS |
| 電話基盤 | Twilio（着信転送先の番号、音声認識`<Gather input="speech">`、読み上げ`<Say>`はAmazon Polly日本語音声） |
| AI | Anthropic Claude API（`/api/voice/turn`が中継） |
| データ保存 | Vercel KV（通話ごとの文字起こし・状態。**音声そのものは録音・保存しない**、テキストのみ） |
| ホスティング | Vercel |
| アクセス制限 | ダッシュボード・管理APIはBasic認証（`proxy.ts`、`DASHBOARD_PASSWORD`） |

## 4. 14モジュールの実現状況（正直な一覧・2026年9月3日 フェーズ2時点）

| # | モジュール | 状況 |
|---|---|---|
| 0 | 着信時アラート・音声ガイド | △ スマホ標準の着信音・バイブ制御は third-party アプリからは限定的。転送ON時の通知はショートカット側で工夫（セクション6） |
| Part1.3 | 冒頭の警告アナウンス | ✅ 実装済み（文言は事実に即して調整。セクション2参照） |
| 1 | ライブ監視 | ✅ ダッシュボードで2秒ごとに文字起こしを表示。現在のモード（ハニーポット/スクリーニング/リバースメンタルケア）も表示。音声そのものの遠隔リアルタイム試聴は未実装（テキストのみ） |
| 2 | ハニーポットAI（時間稼ぎ） | ✅ `lib/honeypot-prompt.ts`。ブロックリスト一致時は「時間稼ぎ優先」ペルソナ |
| 3 | 音声メモ照合・ホワイトリスト | ✅（フェーズ2）ホワイトリストを追加（`lib/whitelist.ts`、Vercel KVのSET）。一致した番号はおとりAIを介さず即座に丁寧な案内で終了する。**「0.5秒で照合」の声紋照合は引き続き未実装**（電話番号の文字列一致のみ。声紋照合は別途音声分析基盤が必要で見送り） |
| 4 | 証拠化支援（Legal Shield） | ✅ キーワード一致による注意フラグ付け＋通話メモの下書き生成（`lib/legal-shield.ts`）。**AIによる法的判定ではない**ことをメモ内に明記 |
| 5 | スマートコントラクトBot | ✅（フェーズ2）`lib/commitment-extractor.ts`の`extractCallerCommitments()`で、相手が提示した期限・金額・急かし文句を検出しダッシュボードに表示。`screening`ペルソナのプロンプトにも「期限・金額を曖昧にさせない」指示を追加 |
| 6 | ブロックリスト照合 | ✅ Vercel KVのSETで管理。ダッシュボードから追加・削除可能。**公開の詐欺番号データベースAPIは現状見つかっていないため、自分で登録していく方式**（セクション9） |
| 7 | 嘘発見・心理バロメーター | ❌ 見送り継続。声の震え・ピッチから嘘を検出する技術は研究レベルでも精度が低く、誤判定が実害（無実の相手を疑う）に直結するため実装していない |
| 8 | 時間浪費ループ | ✅ ハニーポットペルソナのプロンプトに統合（セクション5） |
| 9 | 迷惑営業お断りプロトコル | ✅ ブロックリスト未登録の番号向け「screening」ペルソナとして実装 |
| 10 | ディープフェイク見破り（オレオレ詐欺対策） | ❌ 見送り継続。家族の声を模倣する詐欺の検知は、比較対象となる家族の声紋データが必要でスコープ外 |
| 11 | コンプライアンス地雷検知 | ✅（フェーズ2、当初仕様から再設計）元仕様「ユーザー自身の発言を監視」は転送方式（AIが応答している間、ユーザーは話していない）と相性が悪いため実装不可と判断。代わりに**「おとりAI自身がユーザーに代わって同意・契約してしまっていないか」の事後チェック**に置き換えた（`lib/commitment-extractor.ts`の`extractAiCommitmentWarnings()`）。あわせて`SHARED_SAFETY_RULES`にAIが購入・契約等に同意しないことを明記するルールを追加 |
| 12 | リバースメンタルケア（逆カウンセリング） | ✅（フェーズ2）`lib/tone-detector.ts`で相手の直近発言から攻撃的な口調を検出し、**ブロックリスト一致（ハニーポットモード）の通話でのみ**動的に`honeypot_reverse_care`ペルソナへ切り替える。screeningモード（未確定の相手）には絶対に適用しない設計にして、正当な苦情者を意図せず消耗させるリスクを避けた |
| 13 | リアルタイム声質反転 | ❌ 見送り継続。ボイスチェンジ自体は技術的に可能だが、相手を動揺させる目的での音声加工は用途次第で迷惑防止条例等に抵触しうるグレーな領域。実装しない方針 |
| 14 | SNS拡散エンジン | ✅ ダッシュボードで投稿文を生成・コピー。**自動投稿は実装していない**（セクション2参照） |

**フェーズ2で見送りを継続した3モジュール（7・10・13）について**: 技術的には「それらしく動くもの」を作ること自体は可能だが、(a)精度が低く誤判定の実害が大きい、(b)前提となるデータ（家族の声紋等）がない、(c)法的にグレー、のいずれかに該当するため、フェーズ1での判断を据え置いた。もし挑戦したい場合は、まず7（嘘発見）を「参考程度の目安」であることを画面上に明記した上で試験導入する、といった段階的なアプローチを推奨する。

## 5. おとりAIの人格・安全ルール

`lib/honeypot-prompt.ts`参照。ブロックリスト一致（`blacklisted: true`）かどうかで2種類のペルソナを切り替える。

- **honeypot**（ブロックリスト一致）: 「話の飲み込みが遅く協力的なお年寄り」を演じ、時間を稼ぐ
- **screening**（未登録番号）: 会社名・担当者名・折り返し番号を確認し、不要な営業は丁寧に断る。個人的な用件と分かれば伝言を承って穏便に終える

共通の安全ルール（`SHARED_SAFETY_RULES`）:
- 実在しそうな個人情報（住所・口座番号・カード番号等）は絶対に作らない・言わない
- 差別的発言・本気の脅迫はしない、実在人物へのなりすましはしない
- 相手が本物の緊急事態を訴えている場合はキャラクターを崩して110/119番を案内する

## 6. 設定方法（あなたが行う作業）

### 6-1. Twilioのセットアップ

1. [twilio.com](https://www.twilio.com) でアカウントを作り、支払い方法を登録する
2. 日本語対応の電話番号（またはSMS/Voice対応の番号）を購入する
3. 電話番号の設定画面 →「A call comes in」の Webhook URL に `https://（あなたのVercelドメイン）/api/voice/incoming`（POST）を設定
4. 同じ画面の「Call status changes」に `https://（あなたのVercelドメイン）/api/voice/status`（POST）を設定
5. `TWILIO_ACCOUNT_SID`・`TWILIO_AUTH_TOKEN`（コンソールのトップページで確認できる）を控える

### 6-2. 着信転送の設定（あなたのスマホ側）

- **iOS**: 設定 →電話→着信転送で、Twilioの電話番号への転送をON/OFFできる。Siriショートカットで「この番号に転送」ショートカットを作り、「Hey Siri、防衛モード」等で転送をONにする運用にすると、元のご依頼にあった音声トリガーに近い体験になる
- **Android**: 端末・キャリアの「着信転送」設定（コードは`*21*転送先番号#`が多いが、キャリアにより異なる）。Googleアシスタントのルーティンから、このコードを電話アプリで発信するショートカットを組むことで同様のON/OFF操作ができる
- ⚠️ 転送中は着信そのものがTwilio側に渡るため、あなたの端末には（設定によっては）着信が表示されない。「常に転送」ではなく、「怪しいと思った時だけ手動でON」の運用を推奨

### 6-3. Vercel KVのセットアップ

1. Vercelのプロジェクト →「Storage」→「Create Database」→「KV」を選択
2. 作成すると`KV_REST_API_URL`等が自動的にEnvironment Variablesに追加される
3. ローカル開発では `vercel env pull .env.local` で取得できる

### 6-4. 環境変数の設定

`.env.local.example`を参照し、`ANTHROPIC_API_KEY`・`TWILIO_ACCOUNT_SID`・`TWILIO_AUTH_TOKEN`・`DASHBOARD_PASSWORD`を設定する（Vercelには同じ内容をEnvironment Variablesに登録）。

### 6-5. デプロイ

1. GitHubリポジトリにする
2. Vercelでインポート（Next.jsは自動検出）
3. デプロイ完了後、6-1のWebhook URLを本番ドメインに更新する

## 7. 費用について

- **Twilio**: 番号の月額維持費 + 通話時間課金（分単位）+ 音声認識（Speech Recognition）利用料
- **Anthropic Claude API**: 会話1ターンごとに課金（`max_tokens: 200`に制限済み）
- **Vercel KV**: 無料枠あり、通話量次第で従量課金
- 詐欺師を長時間引き止めるほど、上記すべての費用も伸びる点に注意（`MAX_TURNS = 40`で1通話あたりの上限を設けてある。`app/api/voice/turn/route.ts`）

## 8. 未実装・今後の課題

- Module 0（着信音・バイブの制御）はOS標準機能の制約が大きく、実質的にはショートカット側の工夫に依存する
- Module 3の声紋照合・7（嘘発見）・10（オレオレ詐欺声紋検知）・13（声質反転）は、精度・法的リスクの観点から見送っている（セクション4参照）
- **ホワイトリスト一致時に本人の実電話番号へ自動でかけ直す機能は実装していない**。着信転送がONのままだと、かけ直した電話が再び転送されて無限ループになる恐れがあるため（`app/api/voice/incoming/route.ts`のコメント参照）。ホワイトリスト一致時は「転送を解除してかけ直してください」という案内のみ
- ブロックリスト・ホワイトリストは自分専用（Vercel KV）。複数ユーザーで共有する「クラウドソース型ブロックリスト」にするには、共有DB・投稿の信頼性検証の設計が別途必要
- 通話音声そのものの録音・保存はしていない（テキストの文字起こしのみ）。将来、証拠能力を高めるために音声保存を追加する場合は、双方への録音告知の徹底（現状のアナウンスで一定は満たしている）と、保存データの暗号化・アクセス制御の強化が必要
- 複数ユーザーへの配布（アカウント機能・ユーザーごとのTwilio番号発行・課金）は未着手。現状は「1人が1つのTwilio番号を持つ」単一ユーザー構成
- Module 5（条件確定）・11（AI同意事故検知）はいずれもキーワード一致による簡易検出（AIによる意味理解ではない）。誤検知・見逃しがありうるため、ダッシュボードの表示はあくまで「見落とし防止の補助」として扱うこと

## 9. フェーズ3で追加した3機能（2026年9月3日追記）

ご依頼いただいた「尖った機能」3点について、それぞれ実装前に前提を確認し、以下の形にして組み込んだ。

### 9-1. 特殊詐欺データベース・リアルタイム突合エンジン

**正直な事実確認**: 警視庁・自治体が電話番号単位でリアルタイム照合できる公開APIは、今回も見つからなかった（`lib/blacklist-config.ts`の既存コメント通り）。存在しないAPIに接続するふりをする代わりに、`lib/scam-pattern-detector.ts`で警察庁・国民生活センター等が公表している特殊詐欺の典型的な言い回し（還付金詐欺・オレオレ詐欺・キャッシュカード詐欺盗・架空請求・緊急性/口止め、の5カテゴリ）をパターン化し、**通話中の相手の発言そのものをリアルタイムでスコアリング**する方式にした。スコアが60点（100点満点）を超えると、`app/api/voice/turn/route.ts`がその場でブロックリストへ自動追加し、以降の応答をhoneypotモード（時間稼ぎ優先）へ即時格上げする。ダッシュボードの「条件・注意点をチェック」パネルにスコアと検出カテゴリを表示する。単純な正規表現一致であり、AIによる意味理解ではないため誤検知はありうる（他モジュールと同じ設計方針）。

### 9-2. 通話証拠パッケージ自動PDF生成

`lib/evidence-pdf.ts`で、既存の`buildLegalDraft`（下書きテキスト）・条件抽出の内容をPDF化した（`pdf-lib` + `@pdf-lib/fontkit`、日本語フォントはOFLライセンスのNoto Sans JPを`assets/fonts/`に同梱、約9.6MB）。ダッシュボードから「証拠PDFをダウンロード」で取得できる（`/api/calls/[callSid]/legal-draft/pdf`）。

**⚠️ 正直な注意点**: 内容証明郵便・被害届は郵便局・警察側で書式が決まっている正式書類。このPDFは「時系列・事実関係を整理した下書き」であり、そのまま提出できる正式書類そのものではない（PDF本文にも同じ注意書きを明記）。既存の免責文言（キーワード一致の自動抽出であり法的判定ではない）もそのまま引き継いでいる。

### 9-3. AIの声のトーン調整（「身代わり影武者」の代替実装）

**ご依頼をそのままの形では実装しなかった理由**: (1) 技術的な前提のズレ — このシステムは着信転送方式で、ユーザー本人の声は一切ライン上に流れない（AIの発話は全てテキストからの音声合成）。「ユーザーの声を変換する」という前提の機能自体が対象を持たない。(2) 法的リスクの再燃 — 「官公庁風のトーンで威圧する」は、Module 13（リアルタイム声質反転）で一度「法的にグレー、実装しない方針」と判断済みの内容と実質同じ。相手が詐欺師であっても、AIが公的機関を装うトーンで威圧するのは身分詐称・脅迫隣接のリスクがある。

代わりに`lib/twiml.ts`の`say()`にプロソディ（ピッチ・速度）調整のオプションを追加し、**声のトーンだけ**低め・落ち着いたものにする形にした。ブロックリスト該当番号への冒頭アナウンス（`BLACKLIST_WARNING_LINE`）と、9-1のスコアで新規に格上げされた瞬間の一言（`SCAM_ESCALATION_WARNING_LINE`）で使用する。**話す内容は「この番号は記録されている」という事実の範囲にとどめ、警察・官公庁等の特定機関を名乗らせることは一切していない**（`lib/honeypot-prompt.ts`のSHARED_SAFETY_RULESの「実在の警察官・実在の企業の実在担当者などになりすまさない」ルールをそのまま維持）。
